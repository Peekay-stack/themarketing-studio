- [Change discipline / working agreement](change-discipline-working-agreement.md) -- **READ BEFORE ANY CODE CHANGE.** Sweep blast radius (backend + frontend + siblings) first, plan, keep everything else the same, verify local then live (`tools/smoke.py`, `/selfcheck`, logs) before handing over. Living doc: append dated lessons. Mirrored in the project's CLAUDE.md.
- [Studio Tab-by-Tab doc](studio-tab-by-tab-doc.md) — separate business/build-review artifact (not the wiring audit); now carries verified frontend/backend/linkage detail per tab, and several old findings turned out stale on re-check.
- [Studio save/location audit](studio-save-audit-doc.md) — third audit doc, now Revision 2: POSM/Onground/Video render-tracking fixed via made.py; learning.py tenant-scoping fixed round 92. Video's script/department-card doc-layer gap and Sales enabler channel economics still open.
- [Beta deploy plan](beta-deploy-plan.md) — **LIVE at https://themarketing-studio.com** (Phase 3 DONE 10 Sep: Cloudflare zone active, apex+www `CNAME → themarketing-studio.onrender.com` Proxied, Render custom domains verified + certs issued, SSL Full (Strict), Always Use HTTPS). GitHub `Peekay-stack/themarketing-studio`, Render Starter/Singapore, 10GB disk `/data`, `STUDIO_DATA_DIR=/data`. **Render MCP is set up & working** (hosted `https://mcp.render.com/mcp`, key in git-ignored `.mcp.json` at OPUS root, workspace `tea-dageqc8u01pc73fi55c0`, service `srv-dagfb76k1f9s73cmr8og`). Phase 4 (backups / spend caps / rate-limits) still open.
- [Landing page thread](landing-page-thread.md) — `landing.html` (public marketing page, served at `/`, lives in TWO synced copies). Shipped and LIVE: "THE HUMAN GATE" section, single `<h1>`, meta description, mailto demo CTA; mobile layout pass; the responsive hero visual (diamond diagram + text legend, JS-measured height-matching, no fixed breakpoints) built, iterated through several rounds of user feedback (outline thickness, grid-line weight, tablet ratio, copy polish), and shipped 11 Sep. Nothing open here currently.
- [Account migration plan](account-migration-plan.md) — **corrected 11 Sep: account renews 7–8 Oct, does NOT expire 13 Sep** (earlier urgency was a wrong date). No fire-drill; backups + memory sync are now routine good habit, not a deadline. GitHub (`Peekay-stack/themarketing-studio`) is a standing extra safety net. Still open, not urgent: whether the **Claude Design workspace** is account-bound.
- [Studio work inventory](studio-work-inventory.md) — START HERE for the actual product work. Round 92 (spans several "continued" sections, 5-7 Sep): all 4 producers to structural parity, Video plan-binding, landing page + real login enforcement (`RequireLoginMiddleware`), four-toggle grounding model, Carousel feature, PR Objectives simplification, landing.html "The Marketing Studio" branding. **Round 93 (11 Sep, latest):** client-name-override staleness fully closed (switchBrand now clears clientName + re-fetches bfReady + clears bfVoice); traced Ground Truth (library.py/learning.py) as architecturally brand-blind across a now-5-brand tenant; kicked off the [[brand-grounding-modes-project]] (Stages 1-3 built+live-verified, Stage 4 built+compile-verified only — live testing deferred to 12 Sep morning per the user).
- [Brand-character skill thread](brand-character-skill-thread.md) — `character_skill/SKILL.md` + backend store + six-test approval gate + 12 routes + Ground Truth frontend panel: all BUILT & verified 8 Sep, uncommitted. `character.for_prompt(brand)` now WIRED into generation via `prompts.system_for()` (Social post/carousel, Video script) and `producers._ctx()` (POSM, Onground). Still to do: the character's reference *image* into `/scene-still`/`/posm-image`; candidate/audition image-gen. Sujatha brief is v2 (v1 failed "presentable" test 3), unapproved candidate, no images, not in the store.
- [Brand core / brand key thread](brand-core-brand-key-thread.md) — optional brand-profile input (5 pluggable models: Brand Key/Pyramid/Prism/Wheel/Aaker) = the enduring essence + hard boundaries that guardrail every campaign. Backend + frontend BOTH BUILT & browser-verified 8 Sep (`brandprofile.brand_core_*` folded into `voice_block`; "Brand core" section on the brand-profile screen), all uncommitted. Reaches every `voice_block` caller (Social/Video/house/platform/plan/sales); PR wired 8 Sep via `release_suggestions` source_material (PR generates no prose). Still NOT wired: POSM/Onground brand core (they never call `voice_block`), and brief pre-fill / house stale-cascade / platform boundary-gate.
- [PR/Sales enabler producer roadmap](pr-sales-enabler-producer-roadmap.md) — confirmed intentional: counted as producers on the marketing page for what they already output, not yet at Video/Social/POSM/Onground's structural parity — bringing them there is fulfilling stated intent, not new scope.
- [Design handovers: merge, never replace](design-handovers-merge-never-replace.md) — their app.dc.html is always based on a stale copy; dropping it in whole silently reverts recent fixes.
- [Reload the page after every frontend edit](reload-the-page-after-every-frontend-edit.md) — a fix that "does nothing" is usually a stale bundle, not a logic error.
- [Load the page before claiming a screen works](browser-verification-available.md) — the browser pane works here; endpoint tests alone gave three wrong "ready for testing" verdicts.
- [launch.json lives at the OPUS root](launch-json-lives-at-opus-root.md) — preview_start ignores the inner one, and a space in the exe path silently breaks it.
- [Scratchpad .py files shadow real modules](scratchpad-py-files-shadow-real-modules.md) — a test run from the scratchpad imported hours-old backups of ideas.py and campaign.py; print `mod.__file__` first.
- [Synthetic dicts write real tenant files](synthetic-dicts-write-real-tenant-files.md) — a fixture passed to campaign.save() created a real platforms/t.json; snapshot before the first test, not the obvious one.
- [Prefer cited estimates over blocking](prefer-cited-estimates-over-blocking.md) — stale official data means label an estimate, not refuse a number.
- [app.dc.html is LF, not CRLF](app-dc-html-is-lf-not-crlf.md) — a Python patch on Windows rewrites all 15k lines and destroys the diff that catches Design's reverts.
- [Design contracts need shapes, not names](design-contracts-need-shapes-not-names.md) — naming payload fields without their types cost two live bugs Design could not have avoided.
- [Never put backslashes through a heredoc](never-put-backslashes-through-a-heredoc.md) — a quoted heredoc still eats one level; three of the five failures wrote a broken file silently (21 Sep: regex backslash-b became backspace bytes).
- [Test the case the route describes](test-the-case-the-route-describes.md) — three defects survived four rounds of my own testing because I always verified with pack-only heroes, never the person-led route the user actually chose.
- [Measure the artefact before theorising](measure-the-artefact-before-theorising.md) — "structurally right but still wrong" output had one decisive cause (subject filled 43% of the image area) that no amount of research would have surfaced.
- [Check the SKILL.md before proposing craft changes](check-skill-md-before-craft-changes.md) — six of seven POSM defects were rules the repo had already written and never enforced; the skill also overrode both the web consensus and the user's own suggested fix.
- [Design asks need exact files and instructions](design-asks-need-exact-files-and-instructions.md) — vague "worth a sweep" asks confuse them; find it myself or list exact files/lines.
- [Memory system, not CLAUDE.md, for continuity](memory-system-not-claudemd-for-continuity.md) — "don't restart from 0" means MEMORY.md + studio-work-inventory, already auto-loaded; CLAUDE.md is static reference only.
- [Keep memory mirror in git](keep-memory-mirror-in-git.md) — user wants the whole memory folder mirrored to `<repo>/.claude-memory/` and pushed on EVERY memory change (redundant pre-migration backup). Re-copy the folder + commit + push after any memory write.
- [Wardrobe fixed manually in Shoot Board](wardrobe-fixed-manually-in-shootboard.md) — user is fine editing scene visual text/department notes by hand; don't over-build automatic per-scene wardrobe detection.
- [get_page_text is blind to overlays](get-page-text-blind-to-overlays.md) — it ignores z-index/visual stacking; nearly misread a working login gate as broken because the tool can't see a full-screen overlay covering the content it reported.
- [tar makes unreadable zips here](tar-makes-unreadable-zips-here.md) — `tar -a -c -f x.zip` on this machine produces archives only bsdtar can open (Python/Explorer saw 23 of 12k entries, or "not a zip"). Use a Python `zipfile` script for backups and CRC-test with a different tool.
- [Brand grounding modes project](brand-grounding-modes-project.md) — **PICK UP HERE next session — feature-complete as of Round 15 (16 Sep), nothing known outstanding.** All 5 build stages DONE, and Phases 1-3 of the grounding-leak fix are ALL done too — every scoped gap closed: Phase 2 (`plan.py`'s house pull, `/idea-draft`'s house pull) and Phase 3 (PR's `prDraftRelease`, fixed as a clean refusal) both built and live-verified, plus the Palette/Tone display block now hides while Independent. 15 rounds of live user-testing feedback fixed total. Brief/Strategy/Plan/Producers share ONE Grounded/Independent toggle, backed by a brand-scoped Ground Truth (library.py/learning.py). **SHIPPED 19 Sep with the owner's go-ahead (the old 'nothing committed' instruction is retired)** — the backend modules were committed after live 500s traced to committed main.py calling uncommitted modules. **Pack dropdown (19-21 Sep): picked pack CONFIRMED on live by the owner; Never/design-one/Carousel behaviour observed and understood (Never = no photo attached, a pack may still be drawn). PENDING the owner's live check of the 21 Sep follow-up: with the real pack on file, Automatic + 'Regenerate visuals' on a scene saying 'pouch' should show the real pack (Automatic now recognises pouch/sachet/bag of milk via pack_mode:auto, Social+Carousel only). Per-post 'pack used' label deferred.** **22 Sep: Deploy 1 (Social pack-in-scene) SHIPPED and live-gated clean, commit 170eade -- real-pack chooser, session reference photo, per-post role+label, packscene.py wording (front-only/one-pack/no-marks/sized), scene backstop, readable overlay badge, Rename Enter/Escape fix, brand-scoped /library. Ran 2 more trial images to prove the fixes first. NOT yet verified on live: a real batch with the owner's own login/credits. Owner still to rename the 2 live pack items. NEXT: Deploy 2 Carousel (CTA image slide, 4:5->3:4 ratio fix) -- not yet re-planned. Full detail: brand-grounding-modes-project.md (UPDATE 22 Sep) + the log.** **Tomorrow's likely next step**: either the user brings more live-testing feedback (treat as Round 16+, same live-verify discipline as every round before it), (the ship decision is now made -- see above; follow the change-discipline working agreement for every further change). Local dev server on :8000 will need restarting fresh (backend edits never hot-reload). Full plan in the repo's `BRAND_GROUNDING_MODES_PLAN.md`; every testing round in [[brand-grounding-testing-log]].
- [IMC brief data-ingestion thread](imc-brief-data-ingestion-thread.md) — **Initiative complete** (17 Sep): all 4 phases plus a frontend trigger, all BUILT, live-verified, committed. Plus three real bugs found via live user testing and fixed, all deployed: a production Cloudflare 524 (split ingestion into its own cached `/research-ingest` request), a silent `enrich_with_ai()` failure degrading export sections with zero trace (now logs + surfaces an honest caveat), and NeedScope/CB-CA chart uploads routed by upload slot instead of file extension (PDF charts were silently falling into the research pipeline). Also found+fixed: Dockerfile never set PYTHONUNBUFFERED=1 (real diagnostic gap, now fixed); competitors field lacked the "cite real figures" instruction the backgrounder has (fixed). **18 Sep round**: Competitor Snapshot table gained a deterministic "latest share/HH penetration vs last year" column (`research_parse._yoy_latest()`/`latest_metrics_block()`, real YoY math, uncapped so no competitor's number gets dropped by Synthesize's narrative cap — caught and fixed a real 100x unit bug, raw fractions printed with a bare "%", before it shipped) plus a `w:cantSplit` page-break fix; every exported document (brief, synthesis deck, house/plan/idea platform/guided brief/production bible/call sheet/script) now carries a TMS-logo footer (logo-only after user feedback, no separate URL text, sized up), PR release excepted since it has no export to attach one to yet. **Same-day follow-up**: the real `enrich_with_ai()` export failure that had recurred since 17 Sep is now genuinely root-caused — it had a SECOND silent-failure path (`jsonout.extract()` returning `None` is a normal return, not an exception, so it never hit the previous day's print fix) plus a token budget sized before the backgrounder existed; both fixed and live-verified. **Later same day**: a Hatsun/Arokya "duplicate competitor" finding turned out to be a real, informed brand distinction (user's own category knowledge: shared retail channel, genuinely distinct consumer-facing imagery) — not fixed there, but SMP Defence's own inconsistent merging of the two WAS fixed (one row per named competitor, never combined) and live-verified. A "competitor notes" feature (persistent per-competitor facts on the brand profile, Brand-Core-shaped) was scoped but explicitly held for later. All live-verified end to end with a real minted test session, committed and deployed. **18 Sep closed out clean** — three real fixes shipped and confirmed live (competitor column+footers, footer redesign+real `enrich_with_ai()` root cause, SMP Defence one-row-per-competitor), last deploy `dep-damjk80u01pc7390ge1g` (13:20:26Z), no open bugs known. Still open, deliberately deferred: PPTX/DOCX-native-shape chart rendering needs LibreOffice + a bigger Render instance; untested at the ~20-file ingestion design target; PR-release export+footer; the competitor-notes feature (persistent per-competitor knowledge, Brand-Core-shaped, user said hold for later).
- [Brand grounding testing log](brand-grounding-testing-log.md) — **standing instruction**: append a round to `BRAND_GROUNDING_TESTING_LOG.md` (repo root) after every round of user testing feedback on this toggle, never rewrite past entries. 2 rounds logged as of 12 Sep.
- [Drive the real page with stubs](drive-the-real-page-with-stubs.md) — test a signed-in screen in the pane with no login and no credits: find the app instance, stub fetch, real clicks/keys; found the Rename-Enter bug that way.
